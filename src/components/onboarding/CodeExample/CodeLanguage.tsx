import React, { ReactNode } from "react";

interface Props {
  title: string;
  language: string;
  code: string[];
}

function CodeLanguage(props: Props) {
  return null;
}

export default CodeLanguage;
